apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-collector
  namespace: cdh-bridge
  labels:
    app: event-collector
    version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-collector
  template:
    metadata:
      labels:
        app: event-collector
    spec:
      containers:
        - name: event-collector
          image: ghcr.io/sunilmlodha/cdh-bridge-event-collector:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          env:
            - name: PORT
              value: "3001"
            - name: NODE_ENV
              value: "production"
            - name: LOG_LEVEL
              value: "info"
            - name: MOCK_DATA
              value: "true"
          envFrom:
            - secretRef:
                name: cdh-bridge-secrets
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      imagePullSecrets:
        - name: ghcr-secret   # kubectl create secret docker-registry ghcr-secret ...
---
apiVersion: v1
kind: Service
metadata:
  name: event-collector-service
  namespace: cdh-bridge
spec:
  selector:
    app: event-collector
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
  type: ClusterIP
